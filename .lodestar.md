# Lodestar Context

> Project: lodestar
> Date: 2025-07-25
> Model: claude-opus-4-5
> Session Duration: unknown

## Project Summary

Lodestar is a CLI + MCP tool that synthesizes coding session context into a structured .lodestar.md file, enabling AI assistants and developers to resume sessions with full context. It captures git diffs, decisions, patterns, and open questions across sessions.

**User Segments:**
- Solo founders using AI coding tools
- Developers working with AI pair programmers (Claude, Cursor, Copilot)
- First-time app builders who lose context between sessions

## Integrations

No integrations detected.

## Project Brief Status

- [-] **lodestar init — first-run CLI wizard** — 90% — Implemented with provider selection, key validation, config creation, retry on invalid directory, and skip-prompt-if-valid-config. Minor open question about npm publish readiness.
- [-] **lodestar synthesize — two-diff session synthesis** — 75% — Two-diff architecture (uncommitted + committed since last synthesis) implemented in git.ts and synthesize.ts. Critical integration question still open: does synthesize.ts correctly thread committedDiff and commitLog into prompt template variables. prompts/synthesize.md has uncommitted changes that may affect this.
- [-] **lodestar load — structured context loader** — 60% — File exists per directory structure but no session diff coverage to confirm implementation completeness.
- [-] **lodestar review — browser-based session reader** — 70% — Core committed in 571ed42: src/reader/template.ts (self-contained HTML with progressive disclosure, dark mode, semantic diff panel), src/review.ts server lifecycle, CLI wired in cli.ts. Uncommitted changes still modifying template.ts, schema.ts, review.ts, and prompts/synthesize.md — not yet stable.
- [ ] **lodestar diff — drift detection** — 0% — Phase 1b stub only per CLAUDE.md. Do not implement.

## Decisions

### lodestar review is a short-lived local HTTP server, not a persistent dev server

**Rationale:** Single-use pattern: spin up, open browser, serve once, auto-shutdown after 10 minutes idle or Ctrl+C. Avoids port conflicts, resource leaks, and user confusion about background processes. Not a file watcher, no WebSocket/SSE.
**Files:** src/review.ts, CLAUDE.md

### The HTML reader page is fully self-contained — served as a single string from src/reader/template.ts with all CSS and JS inline

**Rationale:** Works offline, no CDN calls from served page, no build step for the reader, no external dependencies at runtime. Single string export makes the server implementation trivial.
**Files:** src/reader/template.ts

### Progressive disclosure implemented at three levels in the reader: Level 1 always visible (5-second scan), Level 2 expandable sections, Level 3 full detail

**Rationale:** Users need to assess session context at different depths — quick orientation before starting work vs. deep reconstruction of decisions. Three-level disclosure is the community-facing screenshot surface.
**Files:** src/reader/template.ts

### Diff panel in reader computes semantic deltas (new decisions, new deps, resolved questions, new patterns) rather than raw text diffs

**Rationale:** Raw git diff of .lodestar.md is noisy JSON. Semantic comparison (prev.decisions vs curr.decisions by key) surfaces what actually changed in human terms: '3 new decisions, 2 questions resolved'.
**Files:** src/reader/template.ts

### lodestar review added to CLAUDE.md Phase 1a scope as a polish item, sequenced after synthesize/load are stable

**Rationale:** Review is the human-facing surface and community candy screenshot but is non-blocking for core MCP functionality. Explicitly sequenced last within 1a to prevent scope creep during synthesize/load development.
**Files:** CLAUDE.md

### Synthesis now captures two distinct diffs: uncommitted changes (git diff HEAD) and committed changes since the last synthesis commit

**Rationale:** Developers who commit frequently mid-session were losing committed work from the synthesis — the old single git diff HEAD only showed unstaged/staged changes, missing everything already committed. The two-diff approach ensures the full session is captured regardless of commit frequency.
**Files:** src/git.ts, src/synthesize.ts, prompts/synthesize.md

### Last synthesis anchor point is determined by finding the most recent commit that touched .lodestar.md via git log --follow

**Rationale:** The .lodestar.md file itself is the natural anchor — the commit that last updated it is when the previous synthesis happened. No separate state file or tagged commits required.
**Files:** src/git.ts

### When no prior synthesis exists, fall back to showing the last 20 commits as context

**Rationale:** First-run experience: a brand new project has no anchor commit so the committed diff would be empty. Recent commit log gives the AI useful context about project history to seed the first synthesis.
**Files:** src/git.ts

### projectRoot is optional in MCP tools, defaulting to cwd

**Rationale:** Primary use case is an AI assistant calling tools from within the project directory. Requiring explicit path adds friction — defaulting to cwd matches natural usage.
**Files:** src/index.ts

### Init flow skips API key prompt when an existing config is already valid

**Rationale:** Re-running lodestar init to reconfigure tool integrations should not force re-entry of a working API key. Only prompt if provider changed or key is invalid.
**Files:** src/init.ts

### LODESTAR_FILENAME constant '.lodestar.md' extracted into src/git.ts

**Rationale:** Named constant prevents typos and makes future renames a single-point change.
**Files:** src/git.ts

## Patterns

- **Provider abstraction under src/providers/ directory — each provider gets its own file (anthropic.ts, openai.ts, ollama.ts) with a unified interface** — src/providers/
- **Entry points compile to dist/*.js — cli.ts → dist/cli.js, init.ts → dist/init.js, index.ts → dist/index.js** — package.json scripts and bin field
- **History stored in .lodestar.history/ at working directory root, gitignored** — .gitignore, src/history.ts
- **Prompts stored as markdown files in prompts/ directory and loaded at runtime with {{variable_name}} template substitution** — prompts/synthesize.md, src/synthesize.ts
- **Configuration separated into src/config.ts — not inlined into individual modules** — src/config.ts
- **.lodestar.md serves dual purpose: human-readable context document AND anchor marker for determining what changed since last synthesis** — src/git.ts, findLastSynthesisCommit()
- **GitResult discriminated union returned from captureGitSnapshot — callers check result type before accessing snapshot fields** — src/git.ts, src/synthesize.ts
- **Self-contained HTML reader pattern: all CSS, JS, and data inline in a single string export from src/reader/template.ts — no external files, no CDN, no build step for the reader** — src/reader/template.ts, src/review.ts
- **CLI commands loaded lazily via dynamic import() in cli.ts switch statement — avoids loading all command modules on every invocation** — src/cli.ts
- **Semantic diff computed by Set-based key comparison (d.decision, d.package, q.question, p.pattern) rather than index-based or text-based diff** — src/reader/template.ts, renderDiffPanel()

## Dependencies

No dependency changes recorded.

## Rejected Approaches

### Using only git diff HEAD (uncommitted changes) as the sole input to synthesis

**Reason:** Developers who commit mid-session lose all committed work from synthesis. A session with 5 commits and no uncommitted changes would produce an empty diff, generating a useless synthesis.

### Persistent web server or cloud-hosted UI for lodestar review

**Reason:** Explicitly out of scope per CLAUDE.md. A short-lived local server is sufficient and avoids port conflicts, background processes, and infrastructure requirements.

### External CDN dependencies or separate CSS/JS files for the review reader

**Reason:** Requires network access, adds complexity, breaks offline usage. Self-contained single-string HTML is simpler and more robust.

### WebSocket or SSE live reload in the review server

**Reason:** Explicitly rejected per spec. The reader is single-use, not a dev server. Live reload adds complexity for zero user benefit — context doesn't change while you're reading it.

## Open Questions

- [BLOCKING] src/reader/template.ts, src/schema.ts, src/review.ts, and prompts/synthesize.md all have uncommitted modifications. What changed in schema.ts — was LodestarContext extended with new fields, and are these changes required for the review feature to work correctly?
- [BLOCKING] Does src/synthesize.ts correctly thread committedDiff and commitLog from GitSnapshot into the {{committed_diff}} and {{commit_log}} prompt template variables? This is the critical integration point for the two-diff architecture and has not been verified end-to-end.
- [BLOCKING] prompts/synthesize.md has uncommitted changes — does the updated prompt correctly explain the two-diff input structure to the LLM, and do the template variable names match what src/synthesize.ts passes in?
- [non-blocking] Is the lodestar review --diff flag fully wired? It requires loading previous context from .lodestar.history/ — does src/review.ts call history.ts correctly and is historyContext non-null when history exists?
- [non-blocking] What port conflict behavior does src/review.ts implement? CLAUDE.md spec says 'prefer 7357, fallback to any open port' — is this implemented with a try/bind retry loop?
- [non-blocking] The 10-minute idle auto-shutdown in review.ts — is idle defined as no HTTP requests, or no browser tab open? What's the actual implementation?
- [non-blocking] The findLastSynthesisCommit function — if the developer commits .lodestar.md in the same commit as code changes, the committedDiff will exclude that commit's code changes (range is lastSynthCommit..HEAD exclusive). Is this intended or an off-by-one?
- [non-blocking] The README references npm link for installation but the package isn't published to npm yet — is there a plan for npm publish, and does package.json have the right 'files' field to exclude dev artifacts?

## Next Session

- READ src/schema.ts FIRST before touching anything — the uncommitted changes to schema.ts are the highest-risk unknown. If LodestarContext was extended with new fields, every caller (synthesize.ts, load.ts, template.ts, review.ts) may need updating. Understand the schema change before running any builds.
- Commit or stash all five dirty files (.lodestar.md, CLAUDE.md, prompts/synthesize.md, src/reader/template.ts, src/review.ts, src/schema.ts) as a coherent group after reading them — they likely form an in-flight change set. Run npm run build immediately after to verify compilation, then smoke-test with lodestar review in the lodestar project directory itself.
- Verify the two-diff synthesis integration end-to-end: confirm src/synthesize.ts passes committedDiff and commitLog from GitSnapshot into the prompt template variables {{committed_diff}} and {{commit_log}}, then run lodestar synthesize on this project to produce a real synthesis and update .lodestar.md — this anchors the next synthesis and validates the most critical unverified integration point.
- After synthesis is verified, test lodestar review --diff to confirm the history context loading path works (src/review.ts → src/history.ts → historyContext non-null) and that the semantic diff panel renders correctly in the browser.
