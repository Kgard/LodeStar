// lodestar_diff() — STUB ONLY (Phase 1b)

export interface DiffResult {
  error: string;
}

export async function diff(): Promise<DiffResult> {
  return { error: "Phase 1b — not yet implemented" };
}
